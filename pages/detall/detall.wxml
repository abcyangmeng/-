
   <view class="weui-cells weui-cells_after-title">   
            <view class="weui-cell weui-cell_input">   
                <view class="weui-cell__bd"> 
                    <input type='text' class="weui-input" bindinput='bindKeyInput' placeholder="请输入文本" value='{{name}}'/>
                    <button bindtap='addhero' class="weui-btn mini-btn" type="primary" size="mini">发送</button>
                 </view>
             </view>
         </view>   




<view wx:for="{{movies}}" wx:key="{{index}}">

<view><image class='pic' src='{{singer.icon}}' ></image> {{singer.name}}:{{item.title}}</view>
</view>